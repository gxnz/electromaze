package com.multitwo;

import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import com.Main;

public class Level extends Multi2 {

    //switch (mapNum) {
    //case 1:
    //map = maze.getMap(1);
    //maze.buildMaze(root,map);
    //case 2:
    //map = maze.getMap(2);
    //maze.buildMaze(root,map);
    //}
}
