package com.multione;

import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import com.multione.Multi1;

public class Level extends Multi1 {

    //switch (mapNum) {
    //case 1:
    //map = maze.getMap(1);
    //maze.buildMaze(root,map);
    //case 2:
    //map = maze.getMap(2);
    //maze.buildMaze(root,map);
    //}
}
